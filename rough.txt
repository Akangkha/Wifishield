NetShield is a distributed agent–server system.
Agents run on client machines, communicate via gRPC to a central server, and the dashboard visualizes real-time health.
To avoid demo issues, I containerized all components and added Kubernetes manifests so the entire system can be brought up with one command